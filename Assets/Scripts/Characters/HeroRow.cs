using Board;

namespace Characters
{
    public class HeroRow 
    {
        private readonly BoardManager _boardManager;

        // private Queue<>

        public HeroRow(BoardManager boardManager)
        {
            _boardManager = boardManager;
        }
    }
}
